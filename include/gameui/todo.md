#TODO :

* Implementing a coustom tree data structure.
* Each node will be treated as a section.
* As of a tree DS a node can also have it's child nodes
  or sub-nodes.
* Using stack to navigate through these nodes.
* Using method overriding so each nodes can have distinct
  implementations of functions.

* Use of smart pointers over raw pointers.